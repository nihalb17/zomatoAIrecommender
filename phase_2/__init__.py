"""
Phase 2: Core retrieval and ranking engine (non-LLM).

This phase loads the prepared Zomato dataset, applies deterministic
filters based on user preferences (price, locality, rating, cuisine),
and ranks restaurants using simple heuristics.
"""

